# 📹 Система подсчета людей и отслеживания перемещений

Автоматическая система для подсчета людей в помещениях, отслеживания их перемещений между комнатами и визуализации данных на веб-панели с интерактивной картой.

## ✨ Основные возможности

### 🎯 Детекция и трекинг
- **Детекция людей**: Использует YOLOv8 для обнаружения людей на видео
- **Уникальные ID**: Каждому человеку присваивается уникальный ID (p1, p2, p3...)
- **Трекинг**: ByteTrack или упрощенный трекер для отслеживания людей между кадрами
- **Re-Identification**: Сохранение ID при переходе между камерами или временной потере видимости
- **Фильтрация ложных срабатываний**: Автоматическое удаление детекций частей тела (рук, ног)

### 🏠 Управление комнатами
- **Множественные камеры**: Поддержка нескольких камер (USB, IP, RTSP)
- **Отслеживание перемещений**: Автоматическое определение переходов между комнатами
- **Время пребывания**: Расчет и сохранение времени нахождения в каждой комнате
- **История событий**: Логирование всех входов, выходов и перемещений

### 📊 Веб-интерфейс
- **Dashboard**: Главная панель с обзором всех комнат и статистикой
- **Видео стримы**: Live-видео с каждой камеры с отображением ID людей
- **Статистика**: Графики посещаемости, нагрузки системы, количество людей по времени
- **Интерактивная карта**: Визуализация помещений с отображением людей в реальном времени
- **История перемещений**: Таблицы с полной историей всех событий

### 🗺️ Интерактивная карта
- **Real-time позиции**: Отображение людей как точек на карте в реальном времени
- **Траектории движения**: Визуализация путей перемещения людей
- **Расстояния**: Расчет и отображение расстояний в метрах
- **Интерактивность**: Клик по точке для просмотра информации, масштабирование, перемещение
- **Цветовая индикация**: Зеленый (новый вход), желтый (в комнате), красный (покидает)

### 📈 Аналитика
- **Групповой анализ**: Определение групп людей, перемещающихся вместе
- **Статистика по комнатам**: Общее количество посещений, уникальные посетители, среднее время пребывания
- **Графики**: Визуализация данных с помощью Chart.js
- **Экспорт данных**: Экспорт истории перемещений в CSV

### 📸 Скриншоты
- **Автоматическое сохранение**: Скриншоты при входе, выходе и перемещении между комнатами
- **Организация**: Файлы сохраняются с метками времени и ID человека

## 🚀 Быстрый старт

### Требования
- Python 3.8+
- Камера (USB или IP)
- 4GB+ RAM (рекомендуется 8GB)
- GPU (опционально, для ускорения обработки)

### Установка

1. **Клонируйте репозиторий** (или скачайте файлы):
```bash
cd CameraAnalis
```

2. **Создайте виртуальное окружение**:
```bash
python -m venv venv
source venv/bin/activate  # Linux/Mac
# или
venv\Scripts\activate  # Windows
```

3. **Установите зависимости**:
```bash
pip install -r requirements.txt
```

4. **Настройте конфигурацию**:
Откройте `config.py` и настройте:
- Камеры и комнаты (`CAMERAS_CONFIG`)
- Параметры детекции (`DETECTION_CONFIG`)
- Параметры трекинга (`TRACKING_CONFIG`)

Пример для камеры ноутбука:
```python
CAMERAS_CONFIG = [
    {
        "room_name": "Room1",
        "camera_index": 0,  # 0 = камера ноутбука
    },
]
```

5. **Настройте карту помещений** (опционально):
Откройте `room_map_config.json` и настройте расположение комнат:
```json
{
  "rooms": [
    {
      "name": "Room1",
      "x": 50,
      "y": 50,
      "width": 200,
      "height": 150,
      "color": "#667eea",
      "width_meters": 5.0,
      "height_meters": 4.0
    }
  ],
  "canvas_width": 800,
  "canvas_height": 600
}
```

6. **Запустите сервер**:
```bash
python app.py
```

7. **Откройте браузер**:
```
http://localhost:5000
```

## 📁 Структура проекта

```
CameraAnalis/
├── app.py                 # Главный Flask сервер
├── config.py              # Конфигурация системы
├── database.py            # Работа с базой данных
├── detector.py            # Детекция людей (YOLO)
├── tracker.py             # Трекинг людей (ByteTrack)
├── camera_stream.py       # Захват видео с камер
├── room_manager.py        # Управление комнатами и перемещениями
├── screenshot_manager.py   # Сохранение скриншотов
├── group_analyzer.py      # Анализ групп людей
├── room_map_config.json   # Конфигурация карты помещений
├── requirements.txt       # Зависимости Python
├── templates/             # HTML шаблоны
│   ├── index.html         # Dashboard
│   ├── video.html         # Видео стримы
│   ├── statistics.html    # Статистика и графики
│   └── map.html           # Интерактивная карта
├── static/                # Статические файлы
│   ├── css/
│   │   └── style.css      # Стили (SCP Foundation theme)
│   └── js/
│       └── main.js        # JavaScript для обновлений
└── screenshots/           # Сохраненные скриншоты
```

## 🔧 Конфигурация

### Камеры

В `config.py` настройте камеры:

```python
CAMERAS_CONFIG = [
    {
        "room_name": "Room1",
        "camera_index": 0,  # USB камера (0, 1, 2...)
    },
    {
        "room_name": "Room2",
        "camera_index": "rtsp://admin:password@192.168.1.100:554/stream",  # IP камера
    },
]
```

### Детекция

```python
DETECTION_CONFIG = {
    "model": "yolov8n.pt",           # Модель YOLO (nano, small, medium, large, xlarge)
    "confidence_threshold": 0.5,      # Порог уверенности (0.0-1.0)
    "person_class_id": 0,             # ID класса "person" в COCO
}
```

### Трекинг

```python
TRACKING_CONFIG = {
    "track_thresh": 0.5,               # Порог для начала трекинга
    "track_buffer": 150,               # Буфер трекинга (кадры)
    "match_thresh": 0.3,               # Порог сопоставления треков
}
```

## 🌐 API Endpoints

### Основные данные
- `GET /api/rooms` - Статус всех комнат (количество людей, ID)
- `GET /api/movements` - История перемещений
- `GET /api/active_visits` - Активные посещения
- `GET /api/statistics/<room_name>` - Статистика по комнате

### Карта и позиции
- `GET /api/room_map` - Конфигурация карты помещений
- `GET /api/positions` - Позиции всех людей на карте
- `GET /api/trajectories?person_id=p1` - Траектории движения

### Система
- `GET /api/camera_status` - Статус всех камер
- `GET /api/performance_metrics` - Метрики производительности (FPS, история)
- `GET /api/system_load` - Загрузка системы (CPU, RAM)

### Группы
- `GET /api/groups` - Активные группы людей
- `GET /api/group_movements` - История групповых перемещений

### Экспорт
- `GET /api/export/movements` - Экспорт истории перемещений в CSV

## 🎨 Веб-интерфейс

### Dashboard (`/`)
- Обзор всех комнат
- Количество людей в каждой комнате
- Список ID людей
- Активные посещения
- История перемещений
- Групповые перемещения

### Видео (`/video`)
- Live-видео стримы с каждой камеры
- Отображение ID людей на видео
- Статус камер (ONLINE/OFFLINE)
- FPS и разрешение каждой камеры

### Статистика (`/statistics`)
- График посещаемости по времени
- График нагрузки системы (CPU, RAM)
- График количества людей по времени
- Статус всех камер
- Фильтры по комнатам и периодам

### Карта (`/map`)
- Интерактивная карта помещений
- Точки людей в реальном времени
- Траектории движения
- Расстояния в метрах
- Клик по точке для информации
- Масштабирование и перемещение

## 🔍 Фильтрация детекций

Система автоматически фильтрует ложные срабатывания:
- **Минимальный размер**: bbox должен быть не менее 150×150 пикселей
- **Минимальная высота**: не менее 150 пикселей
- **Соотношение сторон**: ширина не должна превышать высоту более чем в 1.2 раза
- **Вложенность**: bbox внутри другого (части тела) отбрасываются
- **Перекрытие**: высокое перекрытие (IoU > 50%) с меньшим bbox отбрасывается

Это предотвращает детекцию рук, ног и других частей тела как отдельных людей.

## 📊 База данных

Система использует SQLite для хранения данных:

- **rooms**: Список комнат
- **persons**: Информация о людях
- **movements**: История перемещений
- **room_visits**: Время пребывания в комнатах
- **group_movements**: Групповые перемещения

## 🔄 WebSocket

Система использует Flask-SocketIO для real-time обновлений:

- `room_update` - Обновление статуса комнат
- `events` - События (вход, выход, перемещение)
- `positions_update` - Обновление позиций на карте

## 🛠️ Решение проблем

### Камера не открывается
- Проверьте, что камера не используется другим приложением
- Для USB камер попробуйте другой индекс (0, 1, 2...)
- Для IP камер проверьте URL и учетные данные

### Низкая производительность
- Используйте более легкую модель YOLO (`yolov8n.pt`)
- Уменьшите разрешение камеры в `camera_stream.py`
- Используйте GPU (если доступна)

### Много ложных срабатываний
- Увеличьте `confidence_threshold` в `config.py`
- Параметры фильтрации можно настроить в `detector.py`

### Карта не отображается
- Проверьте, что `room_map_config.json` существует и содержит валидный JSON
- Проверьте консоль браузера на ошибки
- Убедитесь, что API endpoint `/api/room_map` доступен

## 📝 Лицензия

Этот проект создан для образовательных и исследовательских целей.

## 🤝 Вклад

Приветствуются улучшения и исправления! Создавайте issues и pull requests.

## 📞 Поддержка

При возникновении проблем:
1. Проверьте логи в консоли
2. Убедитесь, что все зависимости установлены
3. Проверьте конфигурацию в `config.py`
4. Проверьте, что камера доступна

---

**Версия**: 2.0  
**Последнее обновление**: 2024
# CameraAnalysis
